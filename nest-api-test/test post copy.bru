meta {
  name: test post copy
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/test
  body: json
  auth: inherit
}

body:json {
  {
    "helo": "dd"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
