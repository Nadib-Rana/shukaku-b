meta {
  name: test post
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/test
  body: json
  auth: inherit
}

body:json {
  {"email": "test@example.com", "name": "Test User"}
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
